# 2026-03-10

## PF-RUNTIME-003A Kernel Contracts

1. Added explicit runtime-kernel contract fields in `src/core/contracts.mjs`:
   - `ActorAddressFields`
   - `RunContextFields`
   - `ArtifactVersionFields`
   - `CommandEnvelopeFields`
   - `ReceiptEnvelopeFields`
2. Added `src/core/KernelEnvelope.mjs` validators for nested envelope shape validation.
3. Extended contract export guard in `scripts/check-typecheck.mjs` to fail on missing kernel contract exports.
4. Added unit tests for kernel envelope validators in `test/unit/core/KernelEnvelope.test.mjs`.
5. Updated canonical spec contract list in `SPEC.md`.
6. Added spec-impact record for PF-RUNTIME-003A phase-A contract introduction.

## PF-GOV-MIG-001 Slice 01 (Gemini command `clarify` to Codex skill)

1. Audited migration dependencies for `.gemini/commands/clarify.toml` against:
   - `.gemini/skills/clarify/SKILL.md`
   - `.gemini/skills/hermeneia/SKILL.md`
   - `.gemini/skills/orchestrator/SKILL.md`
   - `.gemini/settings.json` hook declarations
2. Added Codex-native equivalent at `.codex/skills/clarify/SKILL.md` with:
   - trigger semantics for `clarify` and `/clarify`
   - contextual grounding phase before questioning
   - iterative consequential clarification loop
   - before/after crystallization output contract
   - explicit handoff to `writing-plans` or `orchestrator`
3. Updated governance routing in `docs/governance/local-skill-routing.md` to add intent-crystallization stage (`clarify`, `hermeneia`) ahead of planning.
4. Created `docs/governance/gemini-to-codex-migration-ledger.md` to enforce one-artifact-per-slice migration order and record dependency/behavior deviations.
5. Verification evidence:
   - `npm run check:skills` passed after the slice changes.
   - `npm run verify` passed (docs, governance, typecheck, lint, contract checks, and unit/model tests).

## Risks And Constraints Observed

1. Gemini hook lifecycle (`BeforeTool`, `AfterTool`, `AfterModel`) is not natively transferable to Codex; behavioral parity depends on policy/skill rules rather than runtime hook enforcement.
2. Remaining command/hook migration slices still carry hidden coupling risk until each artifact is dependency-audited and mapped.

## PF-GOV-MIG-001 Slice 02 (Gemini command `council` to Codex skill package)

1. Migrated the source-coupled package by copying `.gemini/skills/agent-council` to `.codex/skills/agent-council` (skill file, config, references, scripts).
2. Rewrote `.codex/skills/agent-council/SKILL.md` for Codex-native behavior:
   - added explicit trigger coverage for `council` and `/council`
   - preserved original `/council` triad perspective contract
   - retained full-council mode with debate, verdict, and secretary minutes
   - replaced Gemini-specific tool calls with Codex-compatible workflow instructions
3. Updated `docs/governance/local-skill-routing.md` with a deliberation stage for contested decisions (`agent-council`, `prothesis`).
4. Updated `docs/governance/gemini-to-codex-migration-ledger.md` with Slice 02 dependency and deviation records.
5. Verification evidence: `npm run verify` passed after Slice 02 changes.

## Additional Risks Observed In Slice 02

1. Optional federated council scripts rely on local CLI availability/auth and Node runtime; behavior can degrade to partial responses if members are missing.
2. Cross-model consensus can create false confidence unless evidence citations and disagreement logs are enforced in every run.

## PF-GOV-MIG-001 Slice 03 (Gemini command `doubt` to Codex skill)

1. Added `.codex/skills/doubt/SKILL.md` as Codex-native emergency re-validation protocol for `!rv`, `!verify`, and `/doubt` triggers.
2. Preserved strict behavior contract:
   - pause forward progress
   - enumerate claims
   - gather fresh evidence per claim
   - publish confirmed/false/unverified verdict
   - correct plan based on verified facts only
3. Updated `docs/governance/local-skill-routing.md` verification stage to include `doubt` before completion claims are trusted.
4. Verification evidence: `npm run verify` passed after Slice 03 changes.

## Additional Risks Observed In Slice 03

1. Without explicit claim enumeration, re-validation can degrade into hand-wavy reassessment and miss concrete factual errors.

## PF-GOV-MIG-001 Slice 04 (Gemini command `factory_init` to Codex skill)

1. Added `.codex/skills/factory-init/SKILL.md` to provide Codex-native planning memory bootstrap.
2. Preserved source command guarantees:
   - initialize `task_plan.md`, `findings.md`, `progress.md` for complex work
   - mandatory reminder to review `docs/learnings.md` and `docs/decisions/`
   - compatibility confirmation phrase: `Factory initialized. External memory is active.`
3. Added a non-breaking safety enhancement: guard against blind overwrite when planning files already contain active context.
4. Updated plan-bootstrap routing to include `factory-init` as a first-step option for new complex tasks.
5. Verification evidence: `npm run verify` passed after Slice 04 changes.

## Additional Risks Observed In Slice 04

1. Template-based initialization can erase active context if overwrite guard is bypassed.

## PF-GOV-MIG-001 Slice 05 (Gemini command `factory_scaffold` to Codex skill)

1. Added `.codex/skills/factory-scaffold/SKILL.md` to scaffold new artifact specs and transparency logs before coding.
2. Preserved source behavior intents:
   - artifact type-driven blueprint (`tool|skill|agent`)
   - create/update `specs/<artifact_name>.md` with success criteria
   - create sequential `notes/FACTORY_RUN_<id>.md`
   - enforce boundedness and failure-mode review gate prior to implementation
3. Remapped missing source references to existing repo standards:
   - `specs/02-workflows.md`
   - `specs/03-tooling-and-skills.md`
   - `specs/04-validation.md`
   - `specs/05-memory-and-documentation.md`
4. Updated `docs/governance/local-skill-routing.md` with "Factory blueprint" stage.
5. Verification evidence: `npm run verify` passed after Slice 05 changes.

## Additional Risks Observed In Slice 05

1. Source-command path drift demonstrates why blind migration from legacy note structures can create silent no-op workflows.

## PF-GOV-MIG-001 Slice 06 (Gemini command `run_local` to Codex skill)

1. Added `.codex/skills/run-local/SKILL.md` for local execution evidence workflow.
2. Source startup command is incompatible with this repository (`ts-node` entrypoint and env-mode assumptions absent).
3. Mapped to repo-native local loop:
   - focused checks: `npm run test:unit`, `npm run test:conformance` (as needed)
   - full gate: `npm run verify`
   - strict gate: `npm run verify:strict`
4. Updated implementation routing to include `run-local` for command evidence capture before readiness claims.
5. Verification evidence: `npm run verify` passed after Slice 06 changes.

## Additional Risks Observed In Slice 06

1. Cross-repo command reuse can silently misconfigure execution semantics when runtime topologies differ.

## PF-GOV-MIG-001 Slice 07 (Gemini command `ship` to Codex skill)

1. Added `.codex/skills/ship/SKILL.md` for release-readiness gating.
2. Preserved and hardened source workflow:
   - git status and scope snapshot
   - verification run
   - transparency log update
   - parity summary against `SPEC.md` and relevant `specs/*.md`
   - ship proposal without implicit push/merge execution
3. Introduced scope-aware verification tiering:
   - `runtime` -> `npm run verify:strict`
   - `governance`/`docs_only` -> `npm run verify`
4. Updated routing policy with explicit release-readiness stage (`ship`).
5. Verification evidence: `npm run verify` passed after Slice 07 changes.

## Additional Risks Observed In Slice 07

1. Release commands that are not tied to scope-classified verification tiers can accidentally downgrade runtime release safety.

## PF-GOV-MIG-001 Slice 08 (Gemini command `verify` to Codex skill)

1. Added `.codex/skills/verify/SKILL.md` for standard verification gate execution.
2. Replaced obsolete source command list with repo-canonical `npm run verify`.
3. Preserved fail-fast behavior: stop on first red and propose minimal fix mapped to canonical docs/specs.
4. Updated verification routing to include `verify` as explicit gate step.
5. Verification evidence: `npm run verify` passed after Slice 08 changes.

## Additional Risks Observed In Slice 08

1. Reconstructing legacy command bundles instead of using canonical gates creates hidden policy drift.

## PF-GOV-MIG-001 Slice 09 (Gemini command `verify_strict` to Codex skill)

1. Added `.codex/skills/verify-strict/SKILL.md` for strict-tier verification execution.
2. Replaced obsolete source strict command set with canonical `npm run verify:strict`.
3. Preserved strict failure semantics:
   - stop on first red
   - perform root-cause analysis before fix proposals
4. Updated verification routing to include explicit strict-tier skill selection.
5. Verification evidence: `npm run verify:strict` passed after Slice 09 changes.

## Additional Risks Observed In Slice 09

1. Strict-gate failures often represent governance drift; treating them as "quick fix" issues can hide systemic regressions.

## PF-GOV-MIG-001 Slice 10 (Gemini command `wrap` to Codex skill)

1. Added `.codex/skills/wrap/SKILL.md` as explicit session closeout protocol.
2. Preserved mandatory zero-context-loss semantics:
   - context snapshot (`git status`, `git diff --stat`)
   - reconciliation from transient files into durable memory
   - verification hygiene before completion claims
   - next-session handoff and risk flagging
3. Updated routing with explicit wrap-stage requirement before material session close.
4. Verification evidence: `npm run verify` passed after Slice 10 changes.

## Additional Risks Observed In Slice 10

1. Without an explicit wrap gate, transient plan/findings state can diverge from durable governance memory.

## PF-GOV-MIG-001 Slice 11 (Gemini hook `epistemic-gate.sh` to Codex guard)

1. Added `scripts/check-epistemic-gaps.mjs` to fail when unresolved `[ ] [Gap:` entries exist in `task_plan.md`.
2. Added `npm run check:epistemic-gaps` and inserted it into canonical `npm run verify`.
3. Updated governance:
   - `docs/governance/guard-registry.md`
   - `docs/governance/verification-tier-policy.md`
   - `docs/governance/local-skill-routing.md` (gap-resolution rule)
4. Verification evidence: `npm run verify:strict` passed after Slice 11 changes.

## Additional Risks Observed In Slice 11

1. Verification-time enforcement is weaker than runtime hook blocking; unresolved gaps can still exist temporarily during active execution until verify is run.

## PF-GOV-MIG-001 Slice 12 (Gemini hook `post-task-lint.sh` to Codex-native flow)

1. Added `scripts/lint-fix.mjs` and command `npm run lint:fix`.
2. Updated `wrap` skill hygiene phase to run `lint:fix` when runtime files changed before final verification.
3. Preserved intent (regular lint cleanup) without hidden per-turn mutations.
4. Verification evidence: `npm run verify` passed after Slice 12 changes.

## Additional Risks Observed In Slice 12

1. Explicit lint-fix invocation is less automatic than source hook behavior; discipline now depends on wrap protocol adherence.

## PF-GOV-MIG-001 Slice 13 (Gemini hook `refresh-cognitive-map.sh` to Codex-native flow)

1. Added `npm run refresh:cognitive-map` alias mapped to canonical `docs:index`.
2. Integrated context-index refresh into `wrap` documentation reconciliation for doc/spec/governance changes.
3. Preserved source intent (keep passive context map fresh) with native index pipeline.
4. Verification evidence: `npm run verify` passed after Slice 13 changes.

## Additional Risks Observed In Slice 13

1. Explicit invocation remains operator-driven outside verify/wrap paths; skipped hygiene steps can still cause temporary index staleness.

## PF-GOV-MIG-001 Slice 14 (Gemini hook `session-state-manager.sh` to Codex utility)

1. Added `scripts/session-state.mjs` with commands:
   - `npm run state:recall`
   - `npm run state:set`
   - `npm run state:verification`
2. Preserved source state schema fields (`activeSkill`, `currentPhase`, `lastVerificationStatus`, `lastVerificationTime`) in `notes/.session-state.json`.
3. Updated `wrap` and routing usage rules to include state recall/persist behavior.
4. Verification evidence: `npm run verify` passed after Slice 14 changes.

## Additional Risks Observed In Slice 14

1. Command-driven state updates rely on workflow discipline; absent runtime hooks, missed invocations can cause stale state snapshots.

## PF-GOV-MIG-001 Slice 15 (Gemini hook `skill-linker.sh` to Codex preflight guard)

1. Added `scripts/check-skill-prereqs.mjs` to validate skill-specific prerequisites.
2. Added `npm run check:skill-prereqs` command.
3. Integrated `executing-plans` preflight invocation and documented rule in routing/guard registry.
4. Included `build-agent-from-scratch` prerequisite check path in the guard utility for future migration slices.
5. Verification evidence: `npm run verify` passed after Slice 15 changes.

## Additional Risks Observed In Slice 15

1. Explicit preflight commands can be skipped if workflows are bypassed; enforcement depends on skill protocol adherence.

## PF-GOV-MIG-001 Slice 16 (Gemini hook `verify-evidence.sh` to Codex verification-state guards)

1. Added verification-state tracking runner: `scripts/run-with-state.mjs`.
2. Added freshness guard: `scripts/check-verification-fresh.mjs`.
3. Refactored verification scripts:
   - `verify:core` + tracked `verify`
   - `verify:strict:core` + tracked `verify:strict`
4. Added `check:verification-fresh` command and integrated completion-time usage into:
   - `verification-before-completion` skill
   - `ship` skill
   - `wrap` skill
5. Updated governance docs/registry/policy for verification-freshness guard coverage.
6. Fixed runtime write failure (`EPERM`) by moving state persistence target from `.codex/session-state.json` to `notes/.session-state.json`.
7. Verification evidence: `npm run verify:strict` passed after state-path correction.

## Additional Risks Observed In Slice 16

1. Freshness enforcement is now command-driven and skill-enforced; if workflows skip these checks, evidence integrity degrades.

## PF-GOV-MIG-001 Slice 17 (Gemini hook `wrap-enforcer.sh` to Codex governance contract)

1. Updated routing policy to mark `wrap` as mandatory before ending material sessions.
2. Added explicit wrap-closeout requirement to `docs/governance/agent-rulebook.md`.
3. Preserved source intent (no closeout without wrap) through explicit policy enforcement.
4. Verification evidence: `npm run verify` passed after Slice 17 changes.

## Additional Risks Observed In Slice 17

1. Without runtime heuristic reminders, enforcement depends on disciplined adherence to governance workflows.

## PF-GOV-MIG-001 Slice 18 (Gemini policy `workspace_safety.toml` to Codex governance policy)

1. Added canonical safety policy config: `config/governance/workspace-safety-policy.json`.
2. Added policy integrity guard: `scripts/check-workspace-safety-policy.mjs` and command `npm run check:workspace-safety`.
3. Added policy doc: `docs/governance/workspace-safety-policy.md`.
4. Integrated workspace safety integrity check into `verify:core`.
5. Updated governance registry and tier policy to include safety-policy gate.

## Additional Risks Observed In Slice 18

1. Local policy guards detect drift but cannot directly intercept runtime commands; platform-level permission controls remain authoritative.

## PF-GOV-MIG-001 Slice 19 (Gemini script `generate-cognitive-map.js` to Codex-native index generator)

1. Enhanced `scripts/generate-passive-context-index.mjs` to absorb source cognitive-map behavior:
   - command catalog from `package.json` scripts
   - local skill catalog from `.codex/skills`
   - markdown summary extraction for `specs/*.md` and `notes/*.md`
   - documentation catalog inventory under `docs/`
2. Added fail-closed safety gate in index generation to prevent accidental index wipeout when critical sources resolve empty.
3. Regenerated `docs/index/passive-context-index.md` with the new Codex-native cognitive-hub sections.
4. Verification evidence: `npm run verify:strict` passed after Slice 19 changes.

## Additional Risks Observed In Slice 19

1. Command catalog now reflects every npm script; if script names become noisy or non-user-facing, cognitive-map signal quality will degrade without curation.

## PF-GOV-MIG-001 Slice 20 (Gemini settings `settings.json` to Codex-native lifecycle policy)

1. Added canonical lifecycle mapping policy: `config/governance/session-automation-policy.json`.
2. Added integrity guard: `scripts/check-session-automation-policy.mjs`.
3. Added command `npm run check:session-automation` and integrated it into `verify:core`.
4. Added governance contract documentation: `docs/governance/session-automation-policy.md`.
5. Updated governance references:
   - `docs/governance/guard-registry.md`
   - `docs/governance/verification-tier-policy.md`
   - `docs/governance/local-skill-routing.md`
   - `docs/governance/agent-rulebook.md`
6. Verification evidence: `npm run verify:strict` passed after Slice 20 changes.
7. Post-reconciliation verification evidence: `npm run verify` passed after updating logs/learnings/ledger artifacts.

## Additional Risks Observed In Slice 20

1. Lifecycle controls are now policy-validated at verify-time, so operators can still drift temporarily during active sessions if they skip protocol steps before verification.

## PF-GOV-MIG-001 Slice 21 (Gemini skill `agent-council/SKILL.md` to Codex-native parity)

1. Re-audited `.gemini/skills/agent-council/SKILL.md` against `.codex/skills/agent-council/SKILL.md`.
2. Restored source-critical behavior in Codex skill:
   - explicit per-persona evidence gathering requirement for external decisions
   - mandatory execution-handoff requirement before ending council output
3. Added explicit synergy map in Codex skill (`orchestrator`, `prothesis`, `writing-plans`, `doubt`) to reduce deliberation-to-execution drop-off.
4. Verification evidence: `npm run verify` passed after Slice 21 changes.
5. Post-reconciliation verification evidence: `npm run verify` passed after updating logs/learnings/ledger artifacts.

## Additional Risks Observed In Slice 21

1. Mandatory handoff remains procedural; if operators bypass skill discipline manually, deliberation can still fail to translate into execution.

## PF-GOV-MIG-001 Slice 22 (Gemini skill `agent-council/council.config.yaml`)

1. Performed file-level diff between source and Codex target config:
   - `.gemini/skills/agent-council/council.config.yaml`
   - `.codex/skills/agent-council/council.config.yaml`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 22 documentation reconciliation.

## Additional Risks Observed In Slice 22

1. Federation member command definitions are static; environment CLI drift can still cause runtime `missing_cli` states even with config parity.

## PF-GOV-MIG-001 Slice 23 (Gemini skill `agent-council/references/config.md`)

1. Performed file-level diff between source and Codex target reference file:
   - `.gemini/skills/agent-council/references/config.md`
   - `.codex/skills/agent-council/references/config.md`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 23 documentation reconciliation.

## PF-GOV-MIG-001 Slice 24 (Gemini skill `agent-council/references/examples.md`)

1. Performed file-level diff between source and Codex target reference file:
   - `.gemini/skills/agent-council/references/examples.md`
   - `.codex/skills/agent-council/references/examples.md`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 24 documentation reconciliation.

## PF-GOV-MIG-001 Slice 25 (Gemini skill `agent-council/references/host-ui.md`)

1. Performed file-level diff between source and Codex target reference file:
   - `.gemini/skills/agent-council/references/host-ui.md`
   - `.codex/skills/agent-council/references/host-ui.md`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 25 documentation reconciliation.

## PF-GOV-MIG-001 Slice 26 (Gemini skill `agent-council/references/overview.md`)

1. Performed file-level diff between source and Codex target reference file:
   - `.gemini/skills/agent-council/references/overview.md`
   - `.codex/skills/agent-council/references/overview.md`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 26 documentation reconciliation.

## PF-GOV-MIG-001 Slice 27 (Gemini skill `agent-council/references/requirements.md`)

1. Performed file-level diff between source and Codex target reference file:
   - `.gemini/skills/agent-council/references/requirements.md`
   - `.codex/skills/agent-council/references/requirements.md`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 27 documentation reconciliation.

## PF-GOV-MIG-001 Slice 28 (Gemini skill `agent-council/references/safety.md`)

1. Performed file-level diff between source and Codex target reference file:
   - `.gemini/skills/agent-council/references/safety.md`
   - `.codex/skills/agent-council/references/safety.md`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 28 documentation reconciliation.

## PF-GOV-MIG-001 Slice 29 (Gemini skill `agent-council/scripts/council-job-worker.ts`)

1. Performed file-level diff between source and Codex target script:
   - `.gemini/skills/agent-council/scripts/council-job-worker.ts`
   - `.codex/skills/agent-council/scripts/council-job-worker.ts`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 29 documentation reconciliation.

## PF-GOV-MIG-001 Slice 30 (Gemini skill `agent-council/scripts/council-job.sh`)

1. Performed file-level diff between source and Codex target script:
   - `.gemini/skills/agent-council/scripts/council-job.sh`
   - `.codex/skills/agent-council/scripts/council-job.sh`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 30 documentation reconciliation.

## PF-GOV-MIG-001 Slice 31 (Gemini skill `agent-council/scripts/council-job.ts`)

1. Performed file-level diff between source and Codex target script:
   - `.gemini/skills/agent-council/scripts/council-job.ts`
   - `.codex/skills/agent-council/scripts/council-job.ts`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 31 documentation reconciliation.

## PF-GOV-MIG-001 Slice 32 (Gemini skill `agent-council/scripts/council.sh`)

1. Performed file-level diff between source and Codex target script:
   - `.gemini/skills/agent-council/scripts/council.sh`
   - `.codex/skills/agent-council/scripts/council.sh`
2. Result: no differences; migration recorded as no-op parity.
3. Verification evidence: `npm run verify` passed after Slice 32 documentation reconciliation.

## PF-GOV-MIG-001 Slice 33 (Gemini skill `brainstorming/SKILL.md`)

1. Added new local skill: `.codex/skills/brainstorming/SKILL.md` to close missing-skill migration gap.
2. Preserved source behavior at protocol level:
   - clarification-first ideation
   - multi-lens evidence gathering
   - strawman options and iterative section-level feedback loop
   - design packaging and explicit handoff to planning/execution skills
3. Added explicit fallbacks for source-coupled, not-yet-migrated skills via available local skills (`prothesis`, `agent-council`, `writing-plans`, `orchestrator`).
4. Updated local routing policy with "Idea shaping (pre-implementation design)" stage including `brainstorming`.
5. Verification evidence: `npm run verify` passed after Slice 33 changes.
6. Post-reconciliation verification evidence: `npm run verify` passed after updating logs/learnings/ledger artifacts.

## PF-GOV-MIG-001 Slice 34 (Gemini skill `build-agent-from-scratch/SKILL.md`)

1. Added new local skill: `.codex/skills/build-agent-from-scratch/SKILL.md`.
2. Preserved source workflow intent:
   - mandatory design interview,
   - specification drafting before implementation,
   - enforced quality gates before sign-off.
3. Mapped source command assumptions to repo-native controls:
   - preflight: `npm run check:skill-prereqs -- --skill build-agent-from-scratch`
   - gates: `npm run check:spec`, `npm run verify` (and `verify:strict` when runtime-impacting)
4. Updated local routing policy with a dedicated "New capability factory (agent/service from zero)" stage.
5. Verification evidence: `npm run verify` passed after Slice 34 changes.
6. Post-reconciliation verification evidence: `npm run verify` passed after updating logs/learnings/ledger artifacts.

## PF-GOV-MIG-001 Slice 35 (Gemini skill `build-agent-from-scratch/assets/SPEC_QUESTIONNAIRE.md`)

1. Added questionnaire asset:
   - `.codex/skills/build-agent-from-scratch/assets/SPEC_QUESTIONNAIRE.md`
2. Updated `.codex/skills/build-agent-from-scratch/SKILL.md` to consume this asset in Phase 1 design interview.
3. Verification evidence: `npm run verify` passed after Slice 35 changes.

## PF-GOV-MIG-001 Slice 36 (Gemini skill `build-agent-from-scratch/assets/SPEC_RUBRIC.md`)

1. Added rubric asset:
   - `.codex/skills/build-agent-from-scratch/assets/SPEC_RUBRIC.md`
2. Updated `.codex/skills/build-agent-from-scratch/SKILL.md` to require rubric self-review before sign-off.
3. Verification evidence: `npm run verify` passed after Slice 36 changes.

## PF-GOV-MIG-001 Slice 37 (Gemini skill `build-agent-from-scratch/scripts/lint_spec.ts`)

1. Added source-parity script copy:
   - `.codex/skills/build-agent-from-scratch/scripts/lint_spec.ts`
2. Added runnable Codex-native equivalent:
   - `scripts/spec-lint.mjs`
   - `npm run spec:lint`
3. Updated build-agent quality-gate order to run `spec:lint` before `check:spec`.
4. Verification evidence: `npm run verify` passed after Slice 37 changes.
5. Additional evidence: `npm run spec:lint` passed and post-reconciliation `npm run verify` passed.

## PF-GOV-MIG-001 Slice 38 (Gemini skill `build-agent-from-scratch/scripts/scaffold_specs.ts`)

1. Added source-parity script copy:
   - `.codex/skills/build-agent-from-scratch/scripts/scaffold_specs.ts`
2. Added runnable Codex-native equivalent:
   - `scripts/spec-scaffold.mjs`
   - `npm run spec:scaffold`
3. Aligned native scaffold output with local canonical conventions:
   - `specs/03-tooling-and-skills.md` and `specs/05-memory-and-documentation.md`
   - `test/integration/conformance`
4. Verification evidence: `npm run verify` passed after Slice 38 changes.

## PF-GOV-MIG-001 Slice 39 (Gemini skill `build-agent-from-scratch/scripts/validate_spec.ts`)

1. Added source-parity script copy:
   - `.codex/skills/build-agent-from-scratch/scripts/validate_spec.ts`
2. Added runnable Codex-native equivalent:
   - `scripts/spec-validate.mjs`
   - `npm run spec:validate`
3. Mapped validation criteria to local canonical `SPEC.md` heading structure to preserve operational value.
4. Updated build-agent quality-gate sequence to include `spec:validate`.
5. Verification evidence:
   - `npm run spec:validate` passed
   - `npm run verify` passed
6. Post-reconciliation verification evidence: `npm run verify` passed after updating logs/learnings/ledger artifacts.

## PF-GOV-MIG-001 Slice 40 (Gemini skill `capture-skill/SKILL.md`)

1. Added new local skill:
   - `.codex/skills/capture-skill/SKILL.md`
2. Mapped source capture workflow to Codex-native local skill paths and checks.
3. Updated routing so `capture-skill` is now part of wrap/memory stage governance.
4. Verification evidence: `npm run verify` passed after Slice 40 changes.

## PF-GOV-MIG-001 Slice 41 (Gemini skill `capture-skill/templates/SKILL_TEMPLATE.md`)

1. Added template parity file:
   - `.codex/skills/capture-skill/templates/SKILL_TEMPLATE.md`
2. This closes the template dependency referenced by local `capture-skill`.
3. Verification evidence: `npm run verify` passed after Slice 41 changes.

## PF-GOV-MIG-001 Slice 42 (Gemini skill `clarify/SKILL.md`)

1. Re-audited source and local clarify skill files:
   - `.gemini/skills/clarify/SKILL.md`
   - `.codex/skills/clarify/SKILL.md`
2. Reconciled local clarify protocol to restore source-critical controls:
   - explicit non-trigger guard for assistant-only low-impact ambiguity,
   - ambiguity category table (Expression/Precision/Coherence/Context),
   - mandatory interactive pause rule that blocks tools until user response.
3. Verification status: covered by shared post-reconciliation run with Slice 43.

## PF-GOV-MIG-001 Slice 43 (Gemini skill `crash-inspector/SKILL.md`)

1. Added new local incident-response skill:
   - `.codex/skills/crash-inspector/SKILL.md`
2. Preserved source behavior in Codex-native protocol:
   - mandatory timestamped crash report in `findings.md`,
   - focused triage before further retries,
   - explicit delegated handoff to repair skills.
3. Updated routing policy to enforce incident capture discipline:
   - added incident-response stage with `crash-inspector`, `systematic-debugging`, `orchestrator`,
   - added usage rule requiring `crash-inspector` after repeated failures or hang cancellations.
4. Verification evidence: `npm run verify` failed in existing contract unit tests (`test/unit/core/KernelEnvelope.test.mjs`) with missing envelope fields (`artifactId`, `taskToken`) unrelated to the skill-migration file surface.

## Additional Risks Observed In Slice 43

1. Incident capture remains process-enforced at skill/routing level; if users bypass the skill path, repeated retry loops are still possible.
2. Migration verification is currently blocked by pre-existing kernel-envelope contract drift in unit fixtures, reducing confidence signaling despite governance/doc changes being localized.

## PF-GOV-MIG-001 Slice 44 (Gemini skill `dev-scan/SKILL.md`)

1. Added new local community-sentiment skill:
   - `.codex/skills/dev-scan/SKILL.md`
2. Preserved source protocol in Codex-native form:
   - parallel multi-community scans (Reddit/HN/Dev.to/Lobsters),
   - mandatory anti-search query for criticism coverage,
   - source-cited synthesis output (consensus, controversy, notable perspectives).
3. Updated routing policy:
   - added "Community sentiment scan" stage (`dev-scan`, `prothesis`, `brainstorming`)
   - added usage rule for developer-opinion/community-reaction requests.
4. Compliance enhancement:
   - normalized section heading `## Protocol` for `clarify`, `crash-inspector`, and `dev-scan` to align with stricter skill-check contracts.
5. Verification evidence: `npm run verify` failed at `check:skills` due broader catalog drift (multiple local skills missing mandatory section contracts).

## Additional Risks Observed In Slice 44

1. Skill catalog contract drift (`check:skills`) now blocks verification earlier than runtime/unit gates; migration confidence is limited until remaining local skills are normalized.

## PF-GOV-MIG-001 Slice 45 (Gemini skill `dispatching-parallel-agents/SKILL.md`)

1. Added new local parallel-dispatch skill:
   - `.codex/skills/dispatching-parallel-agents/SKILL.md`
2. Preserved source behavior in Codex-native form:
   - mandatory independence gate (file/state/dependency overlap checks),
   - focused task packets per domain,
   - parallel investigation mapped to `multi_tool_use.parallel`,
   - deterministic integration before full verification.
3. Updated routing policy:
   - added "Parallel decomposition" stage (`dispatching-parallel-agents`, `orchestrator`, `systematic-debugging`)
   - added usage rule for independent multi-failure situations.
4. Verification evidence: `npm run verify` failed at `check:skills` due broader catalog section-schema drift in pre-existing local skills.

## PF-GOV-MIG-001 Slice 46 (Gemini skill `doubt/SKILL.md`)

1. Performed source-to-local reconciliation:
   - `.gemini/skills/doubt/SKILL.md`
   - `.codex/skills/doubt/SKILL.md`
2. Result: no change required; existing local `doubt` skill already enforces pause, fresh-evidence verification, and explicit verdict/correction loop.

## PF-GOV-MIG-001 Slice 47 (Gemini skill `eval-suite-checklist/SKILL.md`)

1. Reconciled local skill with source matrix requirements:
   - added explicit categories for happy path, edge, adversarial, and system-failure coverage,
   - preserved durable-execution checks (crash/resume, idempotency).
2. Mapped conformance verification to local canonical commands:
   - `npm run test:conformance`
   - `npm run verify:strict` (release/runtime confidence path)
3. Updated routing usage rules to require `eval-suite-checklist` for conformance/eval-suite revisions.
4. Verification evidence: `npm run verify` failed at `check:skills` due remaining catalog-wide section-schema drift in pre-existing local skills.

## PF-GOV-MIG-001 Slice 48 (Gemini skill `eval-suite-checklist/assets/TEST_STRATEGY.md`)

1. Added strategy asset parity file:
   - `.codex/skills/eval-suite-checklist/assets/TEST_STRATEGY.md`
2. Linked asset into local eval-suite skill protocol so category/field requirements are referenced explicitly during matrix setup.
3. Verification evidence: `npm run verify` failed at `check:skills` due unchanged catalog-wide section-schema drift.

## PF-GOV-MIG-001 Slice 49 (Gemini skill `eval-suite-checklist/scripts/lint_conformance.ts`)

1. Added source-parity script copy:
   - `.codex/skills/eval-suite-checklist/scripts/lint_conformance.ts`
2. Added runnable native equivalent:
   - `scripts/conformance-lint.mjs`
   - `npm run conformance:lint`
3. Native adaptation handles local topology difference by skipping with explicit message when source-style JSON case files are absent outside fixtures.
4. Updated eval-suite skill protocol to run conformance lint before `test:conformance`.
5. Verification evidence:
   - `npm run conformance:lint` passed (skip mode)
   - `npm run verify` failed at `check:skills` due existing catalog-wide section-schema drift.

## PF-GOV-MIG-001 Slice 50 (Gemini skill `eval-suite-checklist/scripts/run_conformance.ts`)

1. Added source-parity script copy:
   - `.codex/skills/eval-suite-checklist/scripts/run_conformance.ts`
2. Upgraded native conformance runner:
   - `scripts/run-conformance.mjs` now supports source-style JSON suites with shell-disabled/safety checks and existing `.mjs` conformance tests.
3. Execution evidence:
   - `npm run test:conformance` passed (`.mjs` suite 2/2 green; JSON mode skipped due absent source-style case files outside fixtures).
4. Verification evidence: `npm run verify` failed at `check:skills` due unchanged catalog-wide section-schema drift.

## PF-GOV-MIG-001 Slice 51 (Gemini skill `executing-plans/SKILL.md`)

1. Rewrote local executing-plans protocol to preserve source-critical controls:
   - plan/file freshness checks with go/no-go gate,
   - bounded batch execution with evidence checkpointing,
   - explicit stop conditions for blockers and repeated failures.
2. Preserved local mandatory preflight:
   - `npm run check:skill-prereqs -- --skill executing-plans`
3. Verification evidence: `npm run verify` failed at `check:skills`, but blocker set narrowed (executing-plans no longer appears in missing-section failures).

## PF-GOV-MIG-001 Slice 52 (Gemini skill `finishing-a-development-branch/SKILL.md`)

1. Added new local closeout skill:
   - `.codex/skills/finishing-a-development-branch/SKILL.md`
2. Preserved source closeout controls:
   - mandatory pre-closeout verification,
   - exact four-option decision set (merge/PR/keep/discard),
   - typed confirmation required for discard path.
3. Integrated with local governance:
   - release-readiness routing now includes `finishing-a-development-branch` alongside `ship`,
   - `executing-plans` now hands off to finishing flow + `ship`.
4. Verification evidence: `npm run verify` failed at `check:skills` due remaining legacy skill section-schema drift.

## PF-GOV-MIG-001 Slice 53 (Gemini skill `moonstone-transformer/SKILL.md`)

1. Added new local model-transform skill:
   - `.codex/skills/moonstone-transformer/SKILL.md`
2. Mapped source generation flow to local runtime architecture:
   - scaffold targets aligned to `src/agent/*`, registry/factory wiring, and `.mjs` validators.
3. Updated routing so model-to-runtime scaffold work explicitly enters `moonstone-transformer`.
4. Verification evidence: `npm run verify` failed at `check:skills` due unchanged catalog-wide section-schema drift.

## PF-GOV-MIG-001 Slice 54 (Gemini skill `moonstone-transformer/templates/machine.ts.hbs`)

1. Added template parity file:
   - `.codex/skills/moonstone-transformer/templates/machine.ts.hbs`
2. This closes one of the referenced template dependencies for local `moonstone-transformer`.
3. Verification evidence: `npm run verify` failed at `check:skills` due unchanged catalog-wide section-schema drift.

## PF-GOV-MIG-001 Slice 55 (Gemini skill `moonstone-transformer/templates/schema.ts.hbs`)

1. Added template parity file:
   - `.codex/skills/moonstone-transformer/templates/schema.ts.hbs`
2. This closes the schema-template dependency for local `moonstone-transformer`.

## PF-GOV-MIG-001 Slice 56 (Gemini skill `moonstone-transformer/templates/service.ts.hbs`)

1. Added template parity file:
   - `.codex/skills/moonstone-transformer/templates/service.ts.hbs`
2. This completes the template dependency set referenced by local `moonstone-transformer`.
3. Verification evidence after Slices 55-56:
   - `npm run verify` passed end-to-end (`check:skills` green, `test:unit` 30/30 pass).

## PF-GOV-MIG-001 Slice 57 (Gemini skill `git-craft-commit/SKILL.md`)

1. Added new local commit-hygiene skill:
   - `.codex/skills/git-craft-commit/SKILL.md`
2. Preserved source protocol:
   - logical file-unit staging,
   - conventional commit crafting,
   - no implicit commit/push execution.
3. Updated routing policy:
   - added "Commit preparation" stage,
   - added usage rule requiring `git-craft-commit` for stage/commit preparation requests.
4. Verification evidence: `npm run verify` passed end-to-end (`check:skills` green, `test:unit` 30/30 pass).

## PF-GOV-MIG-001 Slice 58 (Gemini skill `hermeneia/SKILL.md`)

1. Reconciled local hermeneia skill with source protocol depth:
   - explicit expression confirmation and gap-selection phases,
   - consequential option templates,
   - mandatory interactive-pause before tools.
2. Preserved local closeout semantics and handoff into clarification/planning chain.
3. Verification evidence: `npm run verify` passed end-to-end (`check:skills` green, `test:unit` 30/30 pass).

## PF-GOV-MIG-001 Slice 59 (Gemini skill `high-integrity-commit/SKILL.md`)

1. Added new local strict commit-execution skill:
   - `.codex/skills/high-integrity-commit/SKILL.md`
2. Preserved source "iron gate" behavior with canonical local verification:
   - `npm run lint:fix`
   - `npm run verify`
3. Added interactive cluster-approval commit flow and explicit no-implicit-push rule.
4. Updated routing so commit-preparation stage includes `high-integrity-commit`.
5. Verification evidence: `npm run verify` passed end-to-end (`check:skills` green, `test:unit` 30/30 pass).

## PF-GOV-MIG-001 Slice 60 (Gemini skill `katalepsis/SKILL.md`)

1. Added new local comprehension-verification skill:
   - `.codex/skills/katalepsis/SKILL.md`
2. Preserved source comprehension protocol:
   - category entry-point selection,
   - comprehension task registration in `task_plan.md`,
   - Socratic verification loop until convergence.
3. Updated routing:
   - added "Comprehension closure" stage (`katalepsis`, `wrap`)
   - added usage rule for user understanding/walk-through requests.
4. Verification evidence: `npm run verify` passed end-to-end (`check:skills` green, `test:unit` 30/30 pass).

# Work Log - 2026-03-12

## PF-POC-001 Runtime Implementation

- Added POC workflow runtime modules under `src/core/poc`, `src/service/poc`, and `src/provider/poc`.
- Implemented `WorkflowArtifact` contract validation/normalization with declarative condition comparators.
- Implemented retry/idempotency policy with deterministic `(runId,nodeId,attempt)` hashing.
- Implemented append-only file-backed run journal and runtime replay/inspect/resume flows.
- Added connector executors for `action.http` and `action.openai`.
- Added webhook server ingress for artifact trigger execution.
- Added CLI command scripts:
  - `poc:compile`
  - `poc:validate`
  - `poc:run`
  - `poc:serve`
  - `poc:replay`
  - `poc:inspect`
- Added RED-first tests then implementation:
  - artifact/retry/runtime unit tests
  - connector/webhook/replay conformance tests

## PF-POC-001 Governance Reset

- Replaced canonical strategy tracker focus from PF-RUNTIME to PF-POC milestones and actions.
- Added active PF-POC plan/risk docs:
  - `docs/strategy/2026-03-12-pf-poc-001-build-plan.md`
  - `docs/strategy/2026-03-12-pf-poc-001-risk-register.md`
- Updated strategy README to mark PF-RUNTIME-003 docs as superseded context.
- Added supersession markers to legacy PF-RUNTIME-003 build/risk docs.
- Added spec-impact record: `notes/spec-impact/2026-03-12-pf-poc-001-workflow-runtime.md`.

## Verification Notes

- Executed targeted POC tests with local loopback server permissions:
  - `node --test test/unit/poc/*.test.mjs test/integration/conformance/poc-*.conformance.test.mjs`
- Result: pass (12/12).
- Executed strict full-gate verification for runtime PR preparation:
  - `npm run verify:strict`
- Result: pass (all gates green, including conformance suite).

## PF-POC-002 Replay Hardening (Next Slice)

- Added crash-window conformance tests for replay resume gaps:
  - crash after `receipt_recorded`
  - crash after `retry_scheduled`
- Added runtime reconciliation logic during `resume` to recover missing continuation work when journal tail indicates interrupted transition progression.
- Updated strategy trackers:
  - `PF-POC-001` marked done.
  - `PF-POC-002` marked in progress.
  - Added explicit residual risk/action for multi-edge fan-out crash windows.
- Verification:
  - `node --test test/integration/conformance/poc-replay.conformance.test.mjs` passed with new crash-window cases.

## PF-POC-002 Fan-Out Continuation Hardening

- Added crash-injected conformance case for fan-out interruption during `node_enqueued` loop.
- Added continuation lifecycle journaling in runtime:
  - `continuation_planned`
  - `continuation_applied`
  - `continuation_recovered`
- Added resume-time reconciliation for incomplete continuation plans to enqueue only missing downstream nodes.
- Strategy tracking updates:
  - `ACT-004` closed as done.
  - `ACT-008` moved to in progress with evidence.
- Verification:
  - `node --test test/integration/conformance/poc-replay.conformance.test.mjs` passed (fan-out crash case included).
  - `npm run verify:strict` passed after fan-out hardening changes.

## PF-POC-003 Compiler Branching Quality

- Added RED-first compiler unit coverage for:
  - deterministic metadata generation via injectable `now`
  - failure branch emission (`on: failed`)
  - conditional success routing using `exists` comparator for `input.<field>` prompts
  - deterministic compile output for same prompt/options inputs
- Implemented compiler branch heuristics in `PromptCompiler`:
  - `inferFailureBranch(prompt)`
  - `inferInputExistsPath(prompt)`
  - branch topology generation for `openai-present-1` / `openai-missing-1` and optional `openai-failure-1`
  - `metadata.compilerHints` emission for traceability
- Fixed strict JS type-check drift in mixed node arrays by adding explicit JSDoc union types for compiler draft nodes/edges.
- Added compile/validate smoke evidence for branch prompt:
  - `npm run poc:compile -- --prompt "when input.customerId exists post to webhook and on failure send fallback summary" --out .moonstone/artifacts/pf-poc-003-branching.json`
  - `npm run poc:validate -- --artifact .moonstone/artifacts/pf-poc-003-branching.json`
- Verification:
  - `node --test test/unit/poc/PromptCompiler.test.mjs` passed (4/4).
  - `npm run verify:strict` passed (full strict route green).
- PR:
  - Opened `[PF-RUNTIME-014] Improve prompt compiler branching determinism`: https://github.com/chhhhhyoo/Moonstone/pull/5
  - Merged `[PF-RUNTIME-014] Improve prompt compiler branching determinism`: https://github.com/chhhhhyoo/Moonstone/pull/5

## PF-POC-003 Comparator Branching Extension

- Added RED-first compiler unit coverage for comparator-driven prompt branching:
  - numeric comparator condition (`input.amount > 100`)
  - verbal comparator condition (`input.tier equals gold`)
- Extended compiler inference and routing generation:
  - `inferInputComparisonCondition(prompt)` for symbolic and verbal comparators
  - literal parsing for number/boolean/string condition values
  - inverse comparator mapping (`eq<->ne`, `gt<->lte`, `gte<->lt`, `lt<->gte`) so success branches are mutually exclusive
  - conditional branch node shape:
    - `openai-condition-true-1`
    - `openai-condition-false-1`
  - compiler hint enrichment with inferred comparator condition metadata
- Verification:
  - `node --test test/unit/poc/PromptCompiler.test.mjs` passed (6/6).
  - `npm run poc:compile -- --prompt "When input.amount > 100 post order and summarize premium path" --out .moonstone/artifacts/pf-poc-003-comparator.json` passed.
  - `npm run poc:validate -- --artifact .moonstone/artifacts/pf-poc-003-comparator.json` passed.
  - `npm run verify:strict` passed (full strict route green).

## PF-POC-003 Merge Closure And PF-POC-004 Kickoff

- Merged comparator extension PR:
  - `[PF-RUNTIME-015] Expand comparator-aware prompt branching`: https://github.com/chhhhhyoo/Moonstone/pull/6
- Post-merge tracker refresh:
  - `PF-POC-003` marked `done`
  - `ACT-006` marked `done`
  - `POC-007` marked `done`
- Next slice activated:
  - `PF-POC-004` marked `in_progress`
  - `ACT-009` added for compile diagnostics and fallback transparency
  - `POC-008` added for silent-fallback visibility risk

## PF-POC-004 Compile Diagnostics

- Added compile diagnostics contract in compiler output path:
  - `branchMode`
  - applied inference summary
  - fallback warnings
  - generated node IDs
- Preserved API compatibility:
  - new diagnostics-capable `compilePrompt`
  - existing `compilePromptToArtifact` kept as stable artifact-only path
- Extended compile CLI:
  - `poc:compile` now prints diagnostics summary
  - optional `--diagnostics-out <path>` writes full diagnostics JSON report
- Added diagnostics unit coverage:
  - comparator-supported prompt reports `branchMode: comparator`
  - unsupported conditional phrasing reports `branchMode: default` with warning
- Verification:
  - `node --test test/unit/poc/PromptCompiler.test.mjs` passed (8/8).
  - `npm run poc:compile -- --prompt "If customer is vip route premium otherwise route standard" --out .moonstone/artifacts/pf-poc-004-diagnostics.json --diagnostics-out .moonstone/artifacts/pf-poc-004-diagnostics-report.json` passed.
  - `npm run poc:validate -- --artifact .moonstone/artifacts/pf-poc-004-diagnostics.json` passed.
  - `npm run verify:strict` passed (full strict route green).
  - `npm run check:verification-fresh` passed.
